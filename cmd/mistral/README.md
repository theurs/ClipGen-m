# Mistral CLI Utility (ClipGen-m Core)

**Mistral CLI** — это мощный консольный интерфейс для взаимодействия с API Mistral AI на Windows.
Утилита разработана как ядро проекта **ClipGen-m**, но может использоваться как самостоятельный инструмент для терминала. Она поддерживает автоматическую маршрутизацию запросов, обработку файлов, «умные» повторные попытки и полную кастомизацию через конфигурационный файл.

## ✨ Ключевые возможности

*   **Мультимодальность "из коробки"**:
    *   📄 **Текст**: Чат с моделями Mistral Small/Medium/Large.
    *   👁️ **Vision**: Анализ изображений (скриншоты, диаграммы).
    *   🔍 **OCR**: Распознавание текста с PDF и сканов (лучше, чем Vision).
    *   🎙️ **Audio**: Транскрибация голосовых заметок через Voxtral.
*   **Гибкая конфигурация**: Все параметры — от списка моделей до системного промпта и температуры — вынесены в `mistral.conf`. Вы полностью контролируете поведение утилиты.
*   **Умная отказоустойчивость**:
    *   **Ротация ключей**: Если ключ невалиден или исчерпан лимит, утилита мгновенно пробует следующий.
    *   **Перебор моделей**: Если топовая модель (например, `codestral`) недоступна, запрос автоматически уйдет на следующую в вашем списке (например, `mistral-large`).
*   **Windows-Friendly**: Автоматическое определение кодировок (CP866, CP1251, UTF-8). Русский текст в консоли не ломается.
*   **JSON Mode**: Гарантированный валидный JSON на выходе без лишних markdown-оберток.

## 🛠 Установка

Для сборки требуется [Go](https://go.dev/dl/) (версия 1.18+).

1.  **Клонируйте репозиторий и скачайте зависимости**:
    ```bash
    go get golang.org/x/text/encoding/charmap
    ```

2.  **Соберите проект**:
    ```bash
    go build -o mistral.exe ./cmd/mistral
    ```
    *(Или просто `go build`, если вы в корне проекта)*.

## ⚙️ Настройка и Конфигурация

Все настройки хранятся в `%AppData%\clipgen-m\mistral.conf`.

### 1. Быстрый старт (Добавление ключа)
Получите API ключ на [console.mistral.ai](https://console.mistral.ai/) и выполните:
```powershell
mistral.exe -save-key ВАШ_API_КЛЮЧ
```
*Эту команду можно вызвать несколько раз, чтобы добавить запасные ключи для ротации.*

### 2. Продвинутая настройка (`mistral.conf`)
После первого запуска в папке `%AppData%\clipgen-m\` появится файл `mistral.conf`. Вы можете редактировать его вручную:

```json
{
  "api_keys": [
    "secret_key_1",
    "secret_key_2"
  ],
  "base_url": "https://api.mistral.ai",
  "system_prompt": "You are a concise coding assistant...",
  "temperature": 0.7,
  "max_tokens": 8000,
  "models": {
    "code": [
      "devstral-2512",
      "codestral-latest",
      "mistral-large-latest"
    ],
    "general": [
      "mistral-small-latest",
      "mistral-medium-latest"
    ],
    "audio": [
      "voxtral-mini-latest",
      "voxtral-small-latest"
    ]
  }
}
```
*   **models**: Порядок моделей важен. Утилита пробует первую, при ошибке — вторую и т.д.
*   **temperature**: Дефолтная креативность (если не задан флаг `-t`).
*   **system_prompt**: Инструкция для нейросети по умолчанию.

## 🚀 Примеры использования

### 1. Обычный чат
Пайп текста прямо в нейросеть:
```powershell
echo "Как приготовить карбонару?" | mistral.exe
```

### 2. Генерация кода (Mode: Code)
Если в запросе есть слова "код", "json", "функция", утилита сама переключится на модели из списка `code` (например, `devstral`).
```powershell
echo "Напиши скрипт бэкапа базы данных на Python" | mistral.exe
```

### 3. Анализ изображений (Mode: Vision)
```cmd
echo "Какой шрифт используется на этом макете?" | mistral.exe -f "C:\Design\layout.png"
```

### 4. Распознавание документов (Mode: OCR)
Идеально для PDF и сканов документов.
```powershell
mistral.exe -f "C:\Docs\invoice.pdf"
```
*Примечание: Если файл имеет расширение `.pdf`, режим OCR включится автоматически.*

### 5. Аудио в текст (Mode: Audio)
```powershell
echo "Сделай саммари этой встречи" | mistral.exe -f "C:\Records\meeting.mp3"
```
*Утилита использует модели семейства `voxtral`.*

### 6. Анализ контекста из нескольких файлов
```powershell
echo "Почему этот код падает с ошибкой?" | mistral.exe -f "main.go" -f "utils.go" -f "error.log"
```

## 📚 Аргументы командной строки

Приоритет настроек: **Флаг** > **Конфиг** > **Дефолт**.

| Флаг | Описание | Пример |
| :--- | :--- | :--- |
| `-f` | Путь к файлу (Текст, Изображение, Аудио, PDF). Можно указывать несколько раз. | `-f input.txt -f screen.png` |
| `-s` | Системный промпт. Переопределяет значение из конфига. | `-s "Отвечай как пират"` |
| `-j` | JSON режим. Форсирует JSON-структуру в ответе. | `-j` |
| `-t` | Температура (0.0 - 1.0). Переопределяет значение из конфига. | `-t 0.1` (для точности) |
| `-m` | Принудительный режим: `auto`, `general`, `code`, `ocr`, `audio`, `vision`. | `-m ocr` |
| `-v` | Verbose. Вывод подробных логов процесса в консоль. | `-v` |
| `-save-key`| Добавить ключ в конфиг и выйти. | `-save-key secret_...` |

## 🧠 Логика работы (Router & Fallback)

1.  **Анализ входных данных**:
    *   Есть `.mp3/.wav`? → Режим **Audio**.
    *   Есть `.pdf`? → Режим **OCR**.
    *   Есть картинки? → Режим **Vision**.
    *   В тексте есть ключевые слова про код? → Режим **Code**.
    *   Иначе → Режим **General**.

2.  **Выполнение запроса**:
    *   Берется список моделей для выбранного режима из `mistral.conf`.
    *   Берется случайный API ключ.
    *   Идет попытка запроса к первой модели.

3.  **Обработка ошибок**:
    *   `401 Unauthorized` → Ключ удаляется из пула текущей сессии, берется следующий.
    *   `429 Too Many Requests` → Пауза 2 сек, смена ключа. Если ключи кончились — переход к **следующей модели** в списке.
    *   `500/503 Server Error` → Аналогично, переход к следующей модели.

## 📁 Логирование

Если что-то пошло не так, смотрите лог-файл:
`%AppData%\clipgen-m\mistral_err.log`

В файле работает автоматическая ротация (макс. размер 10 МБ). При использовании флага `-v` логи дублируются в `stderr` консоли.