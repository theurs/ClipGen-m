# ClipGen-m ChatUI

## Описание

Приложение ClipGen-m ChatUI - это графический интерфейс для взаимодействия с различными LLM-моделями.

## Возможности

- Поддержка нескольких LLM-провайдеров (Mistral, Gemini, GitHub Copilot, Groq)
- Управление чатами и историями
- Прикрепление файлов и изображений к сообщениям
- Настройка параметров модели (температура, системный промпт, режимы)
- Выбор провайдера LLM для каждого чата

## Поддерживаемые LLM-провайдеры

- Mistral (по умолчанию)
- Google Gemini
- GitHub Copilot/Chat
- Groq

## Унификация команд

Все поддерживаемые LLM-утилиты используют унифицированный набор ключей командной строки с поддержкой как одинарных (-), так и двойных (--) дефисов:
- `-f` / `--f` / `--file` - файлы
- `-s` / `--s` / `--system` / `--system-prompt` - системный промпт
- `-j` / `--j` / `--json` - JSON режим
- `-m` / `--m` / `--mode` - режим модели
- `-t` / `--t` / `--temp` / `--temperature` - температура
- `-v` / `--v` / `--verbose` - подробный вывод
- `--save-key` - сохранение ключа
- `-chat` / `--chat` / `--chat-id` - ID чата

## Сборка

```bash
go build -o ClipGen-m-chatui.exe
```

## Использование

1. Запустите приложение
2. Выберите или создайте новый чат
3. В настройках чата можно выбрать LLM-провайдера и настроить параметры
4. Введите сообщение и нажмите "Отправить" или используйте Ctrl+Enter

## Зависимости

- [walk](https://github.com/lxn/walk) - библиотека для создания Windows GUI