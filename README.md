# 📋 ClipGen-m

**ClipGen-m** — это мощный инструмент автоматизации для Windows, который интегрирует искусственный интеллект (LLM) прямо в ваш буфер обмена.

Больше не нужно переключаться между окнами (Alt+Tab), открывать браузер, логиниться в ChatGPT и копировать ответы туда-сюда. С ClipGen вы просто нажимаете горячую клавишу — и магия происходит там, где вы работаете: в IDE, редакторе кода, Word, Telegram или Figma.

![Platform](https://img.shields.io/badge/platform-Windows-blue)
![Language](https://img.shields.io/badge/language-Go-00ADD8)
![License](https://img.shields.io/badge/license-MIT-green)

---

## ✨ Возможности

### 🚀 Ноль переключений контекста
*   **Глобальные хоткеи:** Работает поверх любых окон.
*   **Мгновенная замена:** Выделите текст -> Нажмите хоткей -> Текст заменится на ответ ИИ.
*   **Режим редактора:** Для длинных ответов (статьи, код) результат откроется в вашем любимом редакторе (VS Code, Notepad++, MarkText).

### 🧠 Мультимодальность (Текст, Картинки, Аудио, Файлы)
ClipGen-m "видит" то, что находится в буфере обмена:
*   **📷 Vision / OCR:** Скопировали скриншот ошибки или таблицу? Нажмите кнопку — получите объяснение или текст (OCR).
*   **🎙️ Аудио (Whisper):** Скопировали файл голосового сообщения (`.mp3`, `.ogg`)? ИИ транскрибирует его и сделает саммари.
*   **📂 Файлы:** Выделили несколько файлов кода в проводнике? ИИ проанализирует их все разом (Code Review, поиск багов).

### 🔌 Модульная архитектура (Backend-agnostic)
Ядро программы запускает внешние CLI-утилиты. Вы сами выбираете "мозги" для своих задач:
*   **Mistral AI:** Отличный баланс качества и OCR (чтение PDF).
*   **Groq:** Реактивная скорость (~800 токенов/сек), поиск в интернете и Whisper Turbo.
*   **GitHub Models:** Бесплатный доступ к GPT-4.

### 🛠 Полезные утилиты
*   **Layout Switcher:** Встроенный аналог Punto Switcher (исправление раскладки `ghbdtn` -> `привет`) одной кнопкой.
*   **Scriptable:** Полная настройка промптов и поведения через YAML.

---

## 📦 Установка

### 1. Сборка из исходников
Вам понадобится установленный [Go 1.23+](https://go.dev/dl/).

Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/clipgen-m.git
cd clipgen-m
```

Запустите скрипт сборки (соберет основное приложение и все AI-клиенты):
```powershell
# Windows
.\build.bat
```
*Или соберите вручную:*
```bash
go build -ldflags "-H=windowsgui" -o ClipGen-m.exe ./cmd/clipgen-m
go build -o mistral.exe ./cmd/mistral
go build -o groqllm.exe ./cmd/groqllm
go build -o github.exe ./cmd/github_llm
```

### 2. Подготовка
Убедитесь, что все `.exe` файлы находятся в одной папке или доступны через PATH.

---

## ⚙️ Конфигурация

При первом запуске `ClipGen-m.exe` создаст файл конфигурации по пути:
`%AppData%\clipgen-m\config.yaml`

### Пример настройки (`config.yaml`)

Вы можете настроить действия под себя. Например, использовать **Groq** для быстрых ответов и **GPT-4o (GitHub)** для сложного кодинга.

```yaml
# Какой редактор открывать для длинных ответов
editor_path: "notepad.exe" 

# Основной движок (можно менять в каждом action отдельно, если дописать логику)
llm_path: "mistral.exe" 

system_prompt: |
  Ты помощник. Отвечай кратко и по делу.

actions:
  # --- Исправление раскладки (Без ИИ) ---
  - name: "Сменить раскладку"
    hotkey: "Pause"
    input_type: "layout_switch"
    output_mode: "replace"

  # --- Быстрый ответ (Mistral) ---
  - name: "Исправить текст"
    hotkey: "Ctrl+F1"
    prompt: "Исправь грамматику: {{.clipboard}}"
    input_type: "text"
    output_mode: "replace"
  
  # --- Работа с картинками (Vision) ---
  - name: "Что на картинке?"
    hotkey: "Ctrl+F6"
    prompt: "Опиши интерфейс на этом скриншоте."
    input_type: "image"
    output_mode: "editor"
```

---

## 🤖 Настройка AI-бэкендов

ClipGen-m использует отдельные CLI-утилиты для общения с нейросетями. Их нужно настроить (добавить API ключи).

### 1. Mistral AI (`mistral.exe`)
Идеален для OCR и общего чата.
1. Получите ключ: [console.mistral.ai](https://console.mistral.ai/)
2. Выполните в консоли:
   ```powershell
   mistral.exe -save-key ВАШ_API_KEY
   ```

### 2. Groq (`groqllm.exe`)
Сверхбыстрый, умеет гуглить и распознавать речь.
1. Получите ключ: [console.groq.com](https://console.groq.com/)
2. Выполните:
   ```powershell
   groqllm.exe -save-key ВАШ_API_KEY
   ```
*Чтобы использовать Groq вместо Mistral, измените `llm_path: "groqllm.exe"` в `config.yaml`.*

### 3. GitHub Models (`github.exe`)
Доступ к GPT-4.
1. Получите токен: [GitHub Marketplace](https://github.com/marketplace/models)
2. Выполните:
   ```powershell
   github.exe -save-key ВАШ_GITHUB_TOKEN
   ```

---

## 🎮 Использование

1. Запустите `ClipGen-m.exe`. В трее появится иконка.
2. Выделите текст или скопируйте файл/картинку.
3. Нажмите настроенный хоткей.


### Примеры сценариев (Default Config):

| Название действия | Что в буфере (Пример) | Хоткей | Что произойдет |
| :--- | :--- | :--- | :--- |
| **Сменить раскладку** | `ghbdtn, vbh!` (Опечатка) | `Pause` | Мгновенно заменит на: `привет, мир!` (Локально). |
| **Исправить текст** | `я зделал это вчера ,но забыл` | `Ctrl+F1` | Заменит текст на: `Я сделал это вчера, но забыл.` (Грамматика). |
| **Выполнить просьбу** | `Придумай 3 заголовка для статьи` | `Ctrl+F2` | **Генерация:** Заменит вашу просьбу на готовый результат (список заголовков). |
| **Перевести и показать** | *[Большой текст на английском]* | `Ctrl+F3` | Переведет текст на русский и **откроет в отдельном окне** редактора (оригинал останется нетронутым). |
| **Перевести и заменить** | `Artificial Intelligence` | `Ctrl+F4` | Мгновенно переведет и **заменит** выделенный фрагмент (`Искусственный Интеллект`). |
| **OCR (Текст с фото)** | *[Скриншот ошибки]* | `Ctrl+F6` | Извлечет текст с картинки и покажет его в редакторе. |
| **Умный анализ** | *[текст картинки файлы в буфере]* | `Ctrl+F8` | **Интерактив:** Спросит у вас "Что сделать?" (напр. *Найди ошибку*), и покажет ответ. |
| **Выкл-Вкл** | | `Ctrl+F12` | Шоткат что бы выключить или включить другие шоткаты. |

---

## 🏗 Архитектура проекта

Проект разделен на микро-утилиты для удобства поддержки и замены компонентов:

```
.
├── cmd/
│   ├── clipgen-m/    # Основное GUI приложение (Tray, Hotkeys, Clipboard)
│   ├── mistral/      # Клиент для Mistral AI (Vision, OCR, Code)
│   ├── groqllm/      # Клиент для Groq (Llama 3, Whisper, Search)
│   ├── github_llm/   # Клиент для GitHub Models (GPT-4o, Azure)
│   └── readdir/      # Утилита для сбора контекста проекта (для разработки)
```

Каждый `*_llm` клиент является самостоятельной CLI-программой, которая принимает промпт через `stdin` или флаги и возвращает ответ в `stdout`. Это позволяет использовать их отдельно от ClipGen в скриптах PowerShell/Bash.

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности см. в файле [LICENSE](LICENSE).

---
*Created by [Your Name]*